# ML Model Training Bundle - Cloud Optimized
# Supports TabPFN (recommended for cloud/GPU) and LightGBM (fallback)

# Core ML dependencies
tabpfn>=2.1.3
lightgbm>=4.6.0
scikit-learn>=1.6.0

# Data processing
pandas>=2.3.0
numpy>=1.24.0
pyarrow>=14.0.0  # For parquet support

# Utilities
tqdm>=4.67.1
typing-extensions>=4.14.0
pytz>=2025.2

# Optional: For data fetching (if retraining with fresh data)
yfinance>=0.2.63
requests>=2.32.4

# For logging
rich>=14.0.0
